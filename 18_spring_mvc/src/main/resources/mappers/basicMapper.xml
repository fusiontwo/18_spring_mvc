<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="basic">

	<!--  
	
		<select id="ex01" resultType="결과타입">
			SELECT
					*
			FROM
					테이블
		</select>
		
		<select id="ex02" resultType="결과타입">
			SELECT
					컬럼
			FROM	
					테이블
			WHERE
					PRIMARY KEY 조건절
		</select>
		
		
		<insert id="ex03" parameterType="입력데이타 타입">
			INSERT INTO
					테이블
			VALUES (
				데이타
			)
		</insert>
		
		
		<update id="ex04" parameterType="입력데이타 타입">
			UPDATE 
					테이블
			SET
					데이타
			WHERE
					조건절
		</update>
		
		
		<delete id="ex05" parameterType="입력데이타 타입">
			DELETE FROM 
					테이블
			WHERE
					조건절
		</delete>
		
	-->
	
</mapper>


